#include <iostream>
#include "Point.h"
#include "Rectangle.h"

void Var(Point pkt, Rectangle rec) {
    if ((pkt.x >= rec.)&&())
}

int main() {

    Point p1("A", 3, 1);
    Rectangle r1("B", 1, 1, 10, 15);

    return 0;
}